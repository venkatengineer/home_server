import 'package:flutter/material.dart';

// ── Color palette ────────────────────────────────────────────────────────────
const Color kBg       = Color(0xFF010A0E);
const Color kPanel    = Color(0xFF061410);
const Color kGreen    = Color(0xFF00FFAA);
const Color kBlue     = Color(0xFF00C8FF);
const Color kYellow   = Color(0xFFFFE000);
const Color kRed      = Color(0xFFFF3366);
const Color kDim      = Color(0xFF3A6657);
const Color kBorder   = Color(0x4000FFA0);
const Color kText     = Color(0xFFC0FFE8);

// ── Refresh interval ─────────────────────────────────────────────────────────
const Duration kRefreshInterval = Duration(seconds: 3);
